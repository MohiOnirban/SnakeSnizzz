#include <stdio.h>

int main(){
    int size;
    printf("size:");
    scanf("%d",&size);

    int matrix [size][size];
    for (int i=0; i<size; i++){
        for (int j=0; j<size; j++){
            printf("Enter %d %d elements;", i+1, j+1);
            scanf("%d",)
        }
    }
    
    
    }